<%= gmaps({
                  :map_options => {
                          :auto_zoom => false,
                          :zoom => 13,
                          :language => I18n.locale,
                          :sensor => false
                  },
                  :markers => {
                          :data => @map
                  }
          })
%>