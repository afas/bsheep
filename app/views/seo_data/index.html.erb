<h1>Listing seo_data</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Keywords</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @seo_data.each do |seo_datum| %>
  <tr>
    <td><%= seo_datum.title %></td>
    <td><%= seo_datum.keywords %></td>
    <td><%= seo_datum.description %></td>
    <td><%= link_to 'Show', seo_datum %></td>
    <td><%= link_to 'Edit', edit_seo_datum_path(seo_datum) %></td>
    <td><%= link_to 'Destroy', seo_datum, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Seo datum', new_seo_datum_path %>
